{
  "name": "mtg-pile",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    // ... (unchanged)
  },
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "CI= react-scripts start",  // Updated line
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:3001"
}
